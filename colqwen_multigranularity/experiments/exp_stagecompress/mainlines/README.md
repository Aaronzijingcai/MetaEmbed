# StageCompress Mainlines

This directory is a clean entry point for the current narrowed research stage.
It uses links into the existing implementation directories so active runs and historical artifacts are not moved.

| Mainline | Directory | Implementation |
|---|---|---|
| Learnable tokens | `learnable_tokens/` | `../llmpre/learnable_tokens/` |
| Homogeneity | `homogeneity/` | `../folder_homo/` |

Do not add another mainline here unless the project scope is intentionally expanded again.
