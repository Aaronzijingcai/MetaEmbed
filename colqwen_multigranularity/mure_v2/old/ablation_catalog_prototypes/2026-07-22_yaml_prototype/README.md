# YAML Prototype

These files are the first generated experiment-catalog prototype. They are
archived because unquoted comma-separated device identifiers were interpreted
as a scalar number by one YAML implementation. The active catalog uses JSON
and the launcher now validates the device count explicitly.
