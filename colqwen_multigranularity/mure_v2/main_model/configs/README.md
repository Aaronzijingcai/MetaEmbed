# Retired Config Format

The earlier descriptive YAML files were moved to
`../../old/main_model_history/pre_cleanup/2026-07-22_pre_cleanup/configs/`.
The runnable main-model definition is now `../experiment.json`; keeping a
second active config format here would make the source of truth ambiguous.
